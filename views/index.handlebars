<h1>Burger Logger!</h1>

<h2>Burger list</h2>

<ul>
    {{#each burgers}}
    {{#unless devoured}}
    <li>
        
        <p>Order: {{burger_name}}<span><button class="change-devour" data-id="{{id}}" data-newdevour="{{devour}}">
                    {{#if devour}}All gone{{else}}Eat me!{{/if}}
                </button></span></p>
    </li>
    {{/unless}}
  {{/each}}

</ul>

<h2>Burgers that have been devoured!</h2>

<ul>
    {{#each burgers}}
    {{#if devoured}}

    {{/if}}
    {{/each}}
</ul>

<h2>Add a burger</h2>
<form class="create-burger">

    <div class="form-group">
        <label for="burger">burger Name:</label>
        <input type="text" id="burger" name="name">
    </div>

    <button type="submit">Add burger</button>
</form>